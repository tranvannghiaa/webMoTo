@model IEnumerable<webMoTo.Models.LoaiMoTo>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLoai)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LoaiId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenLoai)
        </td>

        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.LoaiId }) |
            @Html.ActionLink("Details", "Details", new { id = item.LoaiId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.LoaiId })
        </td>
    </tr>
}

</table>
